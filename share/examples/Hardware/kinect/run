sudo ./x.exe 2>/dev/null
# problem: ctrl-c and the rest of the script doesn't run (but I'm sure I ran it
# once and it worked :/)
sudo chown $USER z.*
sudo chgrp $USER z.*
if [ -f core ]
then
  sudo chown $USER core
  sudo chgrp $USER core
fi
