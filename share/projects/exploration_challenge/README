INSTALL
"""""""

1. Get the hybrid_automaton lib (with the correct branch...)

$ git clone git@animal.ipvs.uni-stuttgart.de:exploration-challenge:rswin
$ cd rswin/hybrid_automaton
$ git checkout haf_abstract
$ mkdir build
$ cd build
$ cmake -DCMAKE_BUILD_TYPE=Debug ..
$ make

2. set the paths for include/lib for the hybrid automaton in the
mlr/share/projects/exploration_challenge/makefile to the appropriate locations,
i.e. ~/src/rswin/hybrid_automaton/include and ~/src/rswin/hybrid_automaton/build

3. build the project

$ make

4. run it

$ ./x.exe
